<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TuVotoCuenta.Pages.Search.ResultDetailPage" xmlns:converters="clr-namespace:TuVotoCuenta.Converters" xmlns:ffimage="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms">
    <ContentPage.Resources>
    </ContentPage.Resources>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Orientation="Vertical" Margin="0,30,0,0">
                <Label Text="Resultados" Margin="0,0,0,10" HorizontalTextAlignment="Center" FontAttributes="Bold" />
                <Label Text="{Binding SelectedEntityText,StringFormat='Entidad: {0}'}" HorizontalTextAlignment="Center" />
                <Label Text="{Binding SelectedMunicipalityText,StringFormat='Municipio: {0}'}" HorizontalTextAlignment="Center" />
                <Label Text="{Binding SelectedLocalityText,StringFormat='Localidad: {0}'}" HorizontalTextAlignment="Center" />
                <BoxView Margin="40,0" HeightRequest="20" BackgroundColor="{StaticResource DarkSelector}" />
                <Frame Margin="0,5,0,0" WidthRequest="254" HeightRequest="354" HorizontalOptions="Center" VerticalOptions="Center" BackgroundColor="Black" OutlineColor="Black" Padding="2">
                    <ffimage:CachedImage DownsampleUseDipUnits="true" DownsampleToViewSize="true" WidthRequest="250" HeightRequest="350" HorizontalOptions="Center" VerticalOptions="Center" Aspect="AspectFill" Source="{Binding DetailResult.PhotoUrl}" />
                </Frame>
                <Grid HorizontalOptions="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75" />
                        <ColumnDefinition Width="75" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Grid.Row="0" TextColor="Green" Text="{Binding DetailResult.UpVotes,StringFormat='+ {0}'}" FontSize="Large" VerticalOptions="Center" HorizontalOptions="Center" />
                    <Label Grid.Column="1" Grid.Row="0" TextColor="Red" Text="{Binding DetailResult.DownVotes,StringFormat='- {0}'}" FontSize="Large" VerticalOptions="Center" HorizontalOptions="Center" />
                    <Button Text="Votar" FontSize="Large" TextColor="Green" Grid.ColumnSpan="2" Grid.Row="1" HorizontalOptions="Center" >
                        <Button.Image Aspect="AspectFill">UpVoteIcon</Button.Image>
                    </Button>
                    <Button Text="Votar" FontSize="Large" TextColor="Red" Grid.ColumnSpan="2" Grid.Row="2" HorizontalOptions="Center" >
                        <Button.Image Aspect="AspectFill">DownVoteIcon</Button.Image>
                    </Button>
                </Grid>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>